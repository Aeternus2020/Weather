rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Public demo project: allow read-only access from the web app.
      allow read: if true;
      allow write: if false;
    }
  }
}
